[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "chatbi-agent-orchestrator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["chatbi-agent-contracts==0.1.0"]

[tool.setuptools.packages.find]
where = ["src"]
