# 备份与恢复

配置 `.env` 后执行 `scripts/backup.ps1 -Name <备份名称>`。备份包含元数据、存储归档及校验清单。

恢复使用 `scripts/restore.ps1 -Name <备份名称>`；需要恢复存储时添加 `-RestoreStorage`。恢复会替换目标内容，请核对项目、数据库及备份名称。脚本会检查清单、版本与并发运行实例。

不要将备份或环境文件提交到 Git。跨版本恢复应使用与备份一致的版本，并先保存当前环境。
